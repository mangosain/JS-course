const obj = {
  title: "Top Gun",
  year: "1986",
  genre: "action",
  star: "Tom Cruise",
  director: "Tony Scott",
};

const { title, director } = obj;

console.log(`Movie: ${title}\nDirected by: ${director}`);
