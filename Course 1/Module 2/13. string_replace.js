const paragraph =
  "js is the backbone of the internet. It was created in 1995. Before js, websites were so boring.";

const updatedParagraph = paragraph.replace(/js/g, "JS");

console.log(updatedParagraph);
